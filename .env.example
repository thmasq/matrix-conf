# Matrix Server Configuration
HOMESERVER_URL=https://your.server.domain
ADMIN_TOKEN={{ADMIN_TOKEN}}

# Optional: Default settings
DEFAULT_PURGE=true
DEFAULT_BLOCK=true
DEFAULT_MESSAGE="This room has been deleted by an administrator"

# API Endpoints (usually don't need to change these)
CLIENT_API_PATH=/_matrix/client/r0
ADMIN_API_PATH=/_synapse/admin/v1
